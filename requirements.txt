# ---------------------------------------------------------------------------
# Python dependencies (installed with: pip install -r requirements.txt)
# For the FULL project stack (language, browser, CI, patterns), see
# PROJECT-STACK.md.
# ---------------------------------------------------------------------------

# --- Core ------------------------------------------------------------------
selenium>=4.18.0      # WebDriver bindings; bundles Selenium Manager (auto driver setup)
pytest>=8.0.0         # Test runner (fixtures, markers, parametrization)

# --- Reporting -------------------------------------------------------------
pytest-html>=4.1.0    # Self-contained HTML test report

# --- Optional --------------------------------------------------------------
# Parallel execution across CPU cores. Uncomment (or `pip install pytest-xdist`)
# to run with:  pytest -n auto
# pytest-xdist>=3.5.0
